export type CompanyDetails = {
  name: string;
  content: string;
};

export type JobDetails = {
  url: string;
  id: string;
  published_on: string;
  title: string;
  description: string;
  location: string;
  salary: string;
};
